#Include "meucodigo.mgc"

global {
    int x;
    int y;
    int z;
}

  function multiplicar(int x, int y){
     int res;
     res = x + y;
  }
  
  main {
     int x, y, z;
     readln(x);
     readln(y);
     z = multiplicar(x,y);
     writeln(z);
  }
